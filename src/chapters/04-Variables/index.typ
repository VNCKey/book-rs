= Tipos de datos

En Rust, los tipos de datos son el concepto fundamental que define la clase
de información que un programa puede almacenar y cómo se representa internamente
en la memoria de la computadora como por ejemplo, si es un número entero, un valor
booleano o una cadena de texto.

Los tipos de datos determinan:

  - Qué valores concretos puede tomar una variable.

  - Qué operaciones son válidas sobre ese valor (no puedes, por ejemplo,
    una cadena de texto).

  - Cuánto espacio de memoria ocupará el valor.
